{
  "name": "artfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step needed'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5"
  }
}
